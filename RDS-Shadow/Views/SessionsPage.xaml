<Page
    x:Class="RDS_Shadow.Views.SessionsPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:controls_legacy="using:CommunityToolkit.WinUI.UI.Controls"
    xmlns:controls="using:CommunityToolkit.WinUI.Controls"
    mc:Ignorable="d" >

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <StackPanel Orientation="Horizontal" Margin="0,0,0,10" Grid.Row="0">
            <Button x:Uid="Sessions_RefreshButton" x:Name="refresh" Click="RefreshButton_Click">
                <Button.Content>
                    <Image Source="/Assets/refresh.ico" Width="16" Height="16"/>
                </Button.Content>
                <ToolTipService.ToolTip>
                    <ToolTip />
                </ToolTipService.ToolTip>
            </Button>
            <TextBox x:Uid="Sessions_FilterTextBox" x:Name="filterUsername" Width="275" Margin="10,0,0,0" PlaceholderText="" TextChanged="filterUsername_TextChanged" />
            <Button x:Uid="Sessions_SendMessageAllButton" x:Name="sendMessageToAllUser" Margin="10,0,0,0">
                <Button.Content>
                    <FontIcon Glyph="&#xE8BD;" />
                </Button.Content>
                <ToolTipService.ToolTip>
                    <ToolTip />
                </ToolTipService.ToolTip>
                <Button.Flyout>
                    <Flyout x:Name="messageAllFlyout" Placement="Full">
                        <Flyout.FlyoutPresenterStyle>
                            <Style TargetType="FlyoutPresenter">
                                <Setter Property="MinWidth" Value="400"/>
                                <Setter Property="Padding" Value="20"/>
                                <Setter Property="BorderThickness" Value="1"/>
                                <Setter Property="CornerRadius" Value="10"/>
                                <Setter Property="Shadow">
                                    <Setter.Value>
                                        <ThemeShadow />
                                    </Setter.Value>
                                </Setter>
                            </Style>
                        </Flyout.FlyoutPresenterStyle>

                        <StackPanel Spacing="20">
                            <TextBlock x:Uid="Sessions_MessageAllTitle" x:Name="Sessions_MessageAllTitle" FontWeight="Bold" FontSize="18" />
                            <TextBox x:Uid="Sessions_MessageAllTextBox" x:Name="messageAllTextBox" TextWrapping="Wrap" AcceptsReturn="True" Height="200" PlaceholderText="" />
                            <Button x:Uid="Sessions_MessageAll_SendButton" x:Name="SendAllButton" Content="" Click="SendAllButton_Click" HorizontalAlignment="Right" />
                        </StackPanel>
                    </Flyout>
                </Button.Flyout>
            </Button>
        </StackPanel>

        <controls_legacy:DataGrid x:Name="shadowingView" Grid.Row="1" AutoGenerateColumns="False" ItemsSource="{x:Bind MyData}" CanUserSortColumns="True" Sorting="shadowingView_Sorting" IsReadOnly="True" DoubleTapped="DataGrid_DoubleTapped" RightTapped="DataGrid_RightTapped">
            <controls_legacy:DataGrid.ContextFlyout>
                <MenuFlyout>
                    <MenuFlyoutItem x:Uid="Sessions_Context_Logout" Click="Abmelden_Click"/>
                    <MenuFlyoutItem x:Uid="Sessions_Context_SendMessage" Click="SendMessageToUser_Click"/>
                </MenuFlyout>
            </controls_legacy:DataGrid.ContextFlyout>
            <FlyoutBase.AttachedFlyout>
                <Flyout x:Name="messageFlyout" Placement="Full">
                    <Flyout.FlyoutPresenterStyle>
                        <Style TargetType="FlyoutPresenter">
                            <Setter Property="VerticalAlignment" Value="Center"/>
                            <!-- Optional: Setze MaxHeight, um die maximale Höhe zu begrenzen -->
                            <Setter Property="MaxHeight" Value="400"/>
                        </Style>
                    </Flyout.FlyoutPresenterStyle>
                    <StackPanel>
                        <TextBox x:Name="messageTextBox" TextWrapping="Wrap" AcceptsReturn="True" Width="350" Height="320"/>
                        <Button x:Uid="Sessions_Message_SendButton" x:Name="SendButton" Content="" Click="SendButton_Click" Margin="0,10,0,0" />
                    </StackPanel>
                </Flyout>
            </FlyoutBase.AttachedFlyout>
            <controls_legacy:DataGrid.Columns>
                <controls_legacy:DataGridTextColumn x:Name="colUsername" Header="Username" Binding="{Binding Username}" />
                <controls_legacy:DataGridTextColumn x:Name="colPoolName" Header="PoolName" Binding="{Binding PoolName}" />
                <controls_legacy:DataGridTextColumn x:Name="colServerName" Header="ServerName" Binding="{Binding ServerName}" />
                <controls_legacy:DataGridTextColumn x:Name="colSessionId" Header="SessionId" Binding="{Binding SessionId}" />
            </controls_legacy:DataGrid.Columns>
        </controls_legacy:DataGrid>
    </Grid>
</Page>
